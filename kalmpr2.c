#include <stdio.h>

#define MINNUM 100
#define MAXNUM 100000
#define MAXK 4

int main()
{
    for (int i = MINNUM; i <= MAXNUM; i++)
    {
        for (int k = 2; k <= MAXK; k++)
        {
            for (int times = 0; times < k; times++)
            {
                // find prime
            }
        }
    }

    return 0;
}